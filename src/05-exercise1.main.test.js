import { getTicketPrice } from './05-exercise1';
test('should ', () => {
    expect(getTicketPrice(13)).toBe(50000);
});
test('should2 ', () => {
    expect(getTicketPrice(13)).toBe(50000);
});
test('should3 ', () => {
    expect(getTicketPrice(13)).toBe(50000);
});
test('should4 ', () => {
    expect(getTicketPrice(13)).toBe(50000);
});